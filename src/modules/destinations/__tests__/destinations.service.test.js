import { describe, it, expect } from 'vitest';

describe('Destinations Service', () => {
  it('deve listar os destinos organizados por categorias', () => {
    expect(true).toBe(true);
  });

  it('deve permitir favoritar um destino', () => {
    expect(true).toBe(true);
  });
});
